# 渲染探针

## A 下划线跨公式配对（ch02 类型）

A1 原样：矩阵 $S^{-1}_{w} S_b$ 的最大特征值为 $J(w)$ 的最大值，矩阵 $S^{-1}_{w} S_b$ 的特征向量。

A2 反引号形式：矩阵 $`S^{-1}_{w} S_b`$ 的最大特征值为 $`J(w)`$ 的最大值，矩阵 $`S^{-1}_{w} S_b`$ 的特征向量。

A3 下标加花括号包裹：矩阵 ${S^{-1}}_{w} S_b$ 的最大特征值为 $J(w)$ 的最大值，矩阵 ${S^{-1}}_{w} S_b$ 的特征向量。

## B 表格单元格内的换行符（ch05 类型）

| 说明 | 公式 |
| --- | --- |
| B1 单反斜杠对 | $\begin{bmatrix} 0 & 0 \\ 0 & 1 \end{bmatrix}$ |
| B2 四反斜杠 | $\begin{bmatrix} 0 & 0 \\\\ 0 & 1 \end{bmatrix}$ |
| B3 用 \cr | $\begin{bmatrix} 0 & 0 \cr 0 & 1 \end{bmatrix}$ |
| B4 反引号形式 | $`\begin{bmatrix} 0 & 0 \\ 0 & 1 \end{bmatrix}`$ |
